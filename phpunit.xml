<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="vendor/autoload.php"
         colors="true"
>
    <testsuites>
        <testsuite name="Unit">
            <directory>tests/Unit</directory>
        </testsuite>
        <testsuite name="Feature">
            <directory>tests/Feature</directory>
        </testsuite>
    </testsuites>
    <source>
        <include>
            <directory>app</directory>
        </include>
    </source>
    <php>
        <env name="APP_ENV" value="testing"/>
        <env name="APP_MAINTENANCE_DRIVER" value="file"/>
        <env name="BCRYPT_ROUNDS" value="4"/>
        <env name="BROADCAST_CONNECTION" value="null"/>
        <env name="CACHE_STORE" value="array"/>
        <!--
            The suite is MySQL, same as local and production. SQLite made
            lockForUpdate() a no-op, hid FK cascade behaviour, and short-
            circuited CSRF. force="true" so a shell that exported
            DB_DATABASE=appoint_manager cannot point the suite at the
            development database — migrate:fresh would wipe it.
        -->
        <env name="DB_CONNECTION" value="mysql" force="true"/>
        <env name="DB_HOST" value="127.0.0.1" force="true"/>
        <!--
            Port is not forced. `scripts/test-mysql84.sh` points the suite at
            compose's 8.4 on 33084 while a laptop mysqld occupies 3306; forcing
            3306 would make that path a no-op. Database name stays forced so a
            shell DB_DATABASE cannot aim migrate:fresh at the salon you are
            working on.
        -->
        <env name="DB_PORT" value="3306"/>
        <env name="DB_DATABASE" value="appoint_manager_test" force="true"/>
        <env name="DB_USERNAME" value="root" force="true"/>
        <env name="DB_PASSWORD" value="" force="true"/>
        <env name="DB_URL" value="" force="true"/>
        <env name="MAIL_MAILER" value="array"/>
        <!--
            Jobs still run inside the HTTP request. Changing this is a
            separate task — see DECISIONS.md, phase 11 follow-up. Do not
            silently flip it: every notification test currently asserts
            side effects that only happen because the job ran inline.
        -->
        <env name="QUEUE_CONNECTION" value="sync"/>
        <env name="SESSION_DRIVER" value="array"/>
        <env name="PULSE_ENABLED" value="false"/>
        <env name="TELESCOPE_ENABLED" value="false"/>
        <env name="NIGHTWATCH_ENABLED" value="false"/>
    </php>
</phpunit>
