{{--
    The empty testimonial section, kept empty on purpose.

    There are no customers yet, so there is no social proof of any kind on this
    surface: no counts, no "trusted by", no quotes, no logos, no ratings, no
    press. Saying so in one line is worth more than the section being absent —
    a page with nothing where the proof goes reads as a page that has not
    thought about it, and this one has.
--}}
<section class="sec sec-tight">
    <div class="wrap split">
        <div class="claim">
            <h2 class="text-17">{{ $heading }}</h2>
        </div>
        <div class="evidence">
            <p class="mt-0 max-w-measure text-14 text-ink-2">
                Nothing here yet. When a real salon writes something we can print, it goes here,
                with their name on it. Not before.
            </p>
        </div>
    </div>
</section>
